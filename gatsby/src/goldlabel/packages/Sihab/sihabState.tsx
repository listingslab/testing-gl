export type SihabShape = {
    showNumber: number
    strFilter: string
    maxResults: number
}

export const sihabState: SihabShape = {
    showNumber: 300,
    maxResults: 10,
    strFilter: "Automatic",
}